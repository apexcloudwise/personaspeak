#!/usr/bin/env python3
import sys
import os

log_path = os.environ.get("MOCK_COMMANDS_LOG")
if log_path:
    with open(log_path, "a") as f:
        f.write(f"FORBIDDEN CONTACT: apksigner {' '.join(sys.argv[1:])}\n")
print("FORBIDDEN: apksigner contact")
sys.exit(99)
