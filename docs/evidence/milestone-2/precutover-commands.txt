# Milestone 2 Pre-Cutover Device Commands
#
# Scaffold only. Device evidence (install, IME registration, real typing,
# fake capture/review/replace, stale refusal, settings launch) is deferred
# to Task 7 Step 3, owned by a separate slice with emulator access.
#
# The command-line gate (verify-milestone-2-precutover.sh) is proven by
# the Task 7 commit and the plan execution log. This file will be filled
# with exact device commands and their outputs when that slice runs.
#
# Commit SHA: <to be filled>
# APK path:   android/keyboard/ime/app/build/outputs/apk/debug/app-debug.apk
# APK SHA-256: <to be filled>
